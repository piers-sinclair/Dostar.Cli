import { screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { default{{ name }}Items } from '@/features/{{ name_lower }}/mocks/handlers';
import { renderWithProviders } from '@/test/utils';
import { {{ name }}List } from './{{ name }}List';

describe('{{ name }}List', () => {
    it('shows a loading indicator while fetching', () => {
        renderWithProviders(<{{ name }}List />);

        expect(screen.getByText('Loading…')).toBeInTheDocument();
    });

    it('renders items returned by the API', async () => {
        renderWithProviders(<{{ name }}List />);

        // TODO: replace with assertions on real rendered content once types and test data are defined
        expect(default{{ name }}Items).toBeDefined();
        await screen.findByRole('list');
    });
});
