import { createFileRoute } from '@tanstack/react-router';
import { {{ name }}Page } from '@/features/{{ name_kebab }}/components/{{ name }}Page';

export const Route = createFileRoute('/{{ name_kebab }}')({
    component: {{ name }}Page,
});
