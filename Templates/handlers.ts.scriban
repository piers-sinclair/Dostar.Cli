import type { RequestHandler } from 'msw';

const BASE = 'http://localhost';
export const {{ name_screaming }}_URL = `${BASE}/api/v1/{{ name_kebab }}`;
export const {{ name_screaming }}_BY_ID_URL = `${BASE}/api/v1/{{ name_kebab }}/:id`;

export const handlers: RequestHandler[] = [];
